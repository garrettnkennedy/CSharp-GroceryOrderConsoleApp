I use chatGPT as a supplementary

This program also acts as a refresher for me, before creating this document I had an error in the visual studio code terminal because I did not create a project for the file to be inside of, which also made it so that I did not receive errors while creating the code(via the red squiggly lines). 

After creating this document, the first error I encountered(not counting warnings like "Converting null literal or possible null value to non-nullable type") was "Cannot implicitly convert type 'string' to 'int' on line 62" the phone number variable was assigned Console.ReadLine(); through online resources I learned about .TryParce and out, and how phone numbers need to be stored inside a long variable instead of a int variable because even though we dont think of phone numbers as values, in a variable they are and the max number a int value can contain is 2,147,483,647, so any phone number that starts with any number above 2 would be too big for a int value.

For the next error, I wrote code for if digitalGroceryCart is empty, but I forgot to add .Count and an int cannot be compared to a list, according to online resources it also turns out that the while loop is a infinite loop, so I changed the parameter of the while loop to (!itemSelectionMatch)

In itemSelection = wrongItemReselect(itemSelectionMatch); I forgot to add itemSelection to the parameters making it (itemSelection, itemSelectionMatch); there was identical code later on that had the same problem which I fixed in the same way.

String itemDeletion was code leftover from previous code that I deleted while creating the first draft that I forgot about and did not delete, so I deleted it

There was a warning in the code return Console.ReadLine(); called "Possible null reference return" and the internet taught me about the null-coalescing operator: ?? which means if the value on the left is null, use the value on the right instead. It says null can be returned if the input stream is closed or you hit EOF which can happen if input is being redirected from a file.

I noticed my previous code string itemSelection; could be added to the previous string userName, Email; to reduce unnecessary lines.

I was taught two methods, I then replaced code like variable = Console.ReadLine(); with variable = ReadRequiredString(MessageHere); or = ReadPhoneNumber(MessageHere); with MessageHere being the previous Console.WriteLine(""); messages that I transferred into the functions parameters.

Now that the errors and warnings are gone, I am first going to try to test run the program. On the first part of the program I entered my name Garrett, it works correctly. When I tried to see what happens when I enter numbers it converts it to the string version so it still works, name can also work as a username so it is not a problem.

When prompted to enter my email I entered a test email: Test@gmail.com, it is supposed to accept an email so long as it has both a @ and either a .com or .net, in testing I noticed that if you enter a valid email the first time it works fine, but if you enter an invalid email like 592DF, when it gives the expected error message prompting you to enter the correct email, it asks you to enter it twice and no matter if your email is valid it will not count it as valid. I relooked at the code and I noticed the code: bool containsAt = Email.Contains("@"); and bool containsDotCom = Email.Contains(".com"); and bool containsDotNet = Email.Contains(".net"); was not also in a while loop that had you re-enter the email, making it an infinite loop. Since it would be in two different locations I turned it into a method/function IsValidEmail which is checked in a while loop.


When prompted to enter my email if you enter an incorrect amount of digits or include values other than digits(like letters) then it gives the correct warning message, but after when it asks you to re-enter the phone number it makes you enter the input twice and it does not accept inputs that are correct, the warning message also stops working in that it always says to enter 10 digits even when you have entered a valid input or when you enter values other then digits, even entering 10 letters gives the wrong warning message. Also if you enter a valid phone number the first time it still makes you enter it twice and does not accept that the input is valid when it is. I found out that the while loop below the phoneNumber = ReadPhoneNumber("message here"); was overwriting the previous code which worked fine, also for an example phone number I was entering does ten 0's and it does not allow leading 0's so it registered it as a single 0, so the while loop would always run the error message inside of it unless you started with a digit other than 0. The code was a previous version of the validation process that I forgot to delete when a method/function replaced it, so I deleted it. To get rid of the problem of the leading 0 I converted the ReadPhoneNumber method from a long to a string, so it no longer needed the code: if (long.TryParse(input, out long result)) as it was no longer a long, instead we could have two if statements that would run if the corresponding error was encountered(using digits or not being 10 digits in length), if an error message is displayed the continue statements re-start the loop.

next I am testing the Add, Delete and Exit functionality, first I noticed that one of the messages added to the parameters was missing a /n so the input line was attached to the end of the message, so I added it. Add works correctly and so does the warning message if you enter input that is not in the list but I realize it is inconvenient that it does not show what a list of the items you added to your digital grocery cart nor does it show it in the Delete option where it would make more sense to show that then the options that can be added. so in the delete section I made it so instead of the product names it instead shows the items currently in the digital grocery cart. I also in the add section created some code that at first gave incorrect input console, Console.WriteLine("Here are your current digital grocery cart items: " + digitalGroceryCart); but because digitalGroceryCart was a List<string> according to the internet it automatically calls the ToString() inbuilt method which does not give the list of the contents but instead returns the type name of the object, which is why the output looked so strange: System.Collections.Generic.List'1[System.String]      it then taught me that string.Join() can be used in this case as it combines multiple strings into one string and is called like normal. I also re-arranged if (digitalGroceryCart.Count == 0) so that it would encapsulate the code inside the optionSelection == "Delete" or "delete" if statement, so that it would not prompt you to delete an item if your digital grocery cart was empty. Instead giving you a warning message.

Next I tested the Exit option but it did not work. I forgot that if one side is true and the other is false it will default to true instead of false, and since a single input cannot have both an uppercase and lowercase first letter, it always ran as true in an infinite loop, so I switched || to && to fix it.

I then noticed that when the Exit statement ran it would output the else statements warning message above the correct output because the else statement did not have any parameters to stop it, so I changed the else statement to else if (optionSelection != "Exit" && optionSelection != "exit") so that it would output correctly.

 I removed the else{} in WrongItemSelection as it will still run the return itemSelection with the exact same functionality, else { } was technically pointless code, also in the Add if statement I converted itemSelectionMatch != true to itemSelectionMatch == false for readability.